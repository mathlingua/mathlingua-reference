# Is Question Keyword

`is?` and `is_not?` are predicate forms.

```mlg
x is? \set
x is_not? \empty.set
```

They ask whether the subject satisfies or does not satisfy the type expression. In clauses, the checker still validates that the subject is declared and that the referenced type exists.
