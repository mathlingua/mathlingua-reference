# Labeled Expressions

A labeled expression attaches a label to an expression.

```mlg
(x + y)[:sum:]
(.x + y.)[:normalized:]
```

Labels are metadata on expressions. They use the `[:...:]` token form and are distinct from structural label headers such as `[theorem.name]`.
