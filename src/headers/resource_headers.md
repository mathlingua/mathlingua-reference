# Resource Headers

Resource headers begin with `$` and are used by `Resource:` groups and `References:` sections.

```mlg
[$zfc.foundations]
Resource:
. title: "ZFC Foundations"
. url: "https://example.com"
```

A `References:` section contains resource headers, not quoted text.
