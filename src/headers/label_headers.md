# Label Headers

Label headers are bracketed names used for theorem-like labels and optional nested labels.

```mlg
[theorem.ordinal.trichotomy]
Theorem:
then: ...

[case.left]
allOf:
. P
. Q
```

Dotted labels are useful because renderers can turn dots into spaces for fallback titles, while source stays precise.
