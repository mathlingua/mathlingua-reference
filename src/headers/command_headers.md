# Command Headers

```mlg
\some.command
\foo{f(x_)}
\foo{X ::= {x_ : ...}}
\foo{G ::= (X, *, e)}
\foo{G ::= (X ::= (a, b), f(x_))}
\foo{a, b}(x_)
```

Note the `:?` marks the parameter as optional.
```mlg
\function:?on{A}:?to{B}
```
