# `mlg init`

`mlg init` creates a starter MathLingua collection in the current directory.

Use it when beginning a new collection. After initialization, run:

```text
mlg check
mlg view
```

The initialized files are normal `.mlg` source files and can be edited directly.
