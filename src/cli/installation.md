# Installation

Build or install the `mlg` executable from the main MathLingua repository. During local development, the usual workflow is to build the Rust workspace and run the resulting binary from a collection directory.

After installation, verify that the command is available:

```text
mlg version
```

If the shell cannot find `mlg`, add the directory containing the executable to your `PATH` or run it by absolute path.
