# Negative Decimal

A `negative:` group with a `decimal:` section records the `negative decimal` classification.

```mlg
[negative.decimal]
negative:
decimal:
is: "negative decimal"
```

These groups are open-text metadata. The heading is optional and, when present, is a label header.
