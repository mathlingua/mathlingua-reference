# Positive Int

A `positive:` group with a `int:` section records the `positive integer` classification.

```mlg
[positive.integer]
positive:
int:
is: "positive integer"
```

These groups are open-text metadata. The heading is optional and, when present, is a label header.
