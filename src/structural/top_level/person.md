# Person

```mlg
[AuthorHeader]
Person: <OpenText>+
biography?: <OpenText>
```

The person's display name is written directly in the `Person:` section.
`biography:` is optional and renders as Markdown card text in `mlg view`.
