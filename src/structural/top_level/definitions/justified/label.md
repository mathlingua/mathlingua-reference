# Label

A `label:` justification item contains optional label text, optional `by:` text, and a required `comment:`.

```mlg
Justified:
. label: "Pairing"
  by: "ZFC"
  comment: "The axiom supplies the required set."
```

The optional heading, when present, is a label header.
