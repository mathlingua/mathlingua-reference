# Description

```mlg
[LabelHeader]?
description: <OpenText>
```

The description is quoted prose that may span multiple lines. The renderer
unindents it and shows it above the card as page prose; the card details refer
to it as `<see above>`.
